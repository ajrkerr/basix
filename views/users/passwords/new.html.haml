%article
  %h2 Forgot your password?

  %p Please provide your email address and we will email you a link to reset your password.

  = semantic_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| 
    = devise_error_messages! 

    = f.input :email 

    .btn-group
      = f.submit "Send me reset password instructions", :class => 'btn'

  = render :partial => "users/shared/links" 